/** Product unit barcode length used across ops new-inspection flows. */
export const OPS_BARCODE_LEN = 16;

/** Shown when the bottom EAN-13 (~13 digits) is scanned instead of the top code. */
export const OPS_BARCODE_SCAN_TOP_HINT =
  "Please scan the top code to continue.";
