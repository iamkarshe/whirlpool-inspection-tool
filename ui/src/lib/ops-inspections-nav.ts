import { ClipboardList, type LucideIcon } from "lucide-react";

/** Document title + bottom-nav label for manager `/ops/team` and operator `/ops/data` KPI hubs. */
export const OPS_INSPECTIONS_TITLE = "Inspections";

/** Same icon in manager and operator Ops layouts for the Inspections tab. */
export const OPS_INSPECTIONS_NAV_ICON: LucideIcon = ClipboardList;
